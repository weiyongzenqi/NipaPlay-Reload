# 项目进度记录

> Claude Code 每次开始工作时应首先读取此文件。

## Git 状态快照 (2026-04-27)

```
分支: main
最新提交: 待提交

本次新增修改:
  lib/pages/webdav_browser_page.dart       # 排序、返回逻辑、多行文件名、观看历史
  lib/providers/webdav_quick_access_provider.dart  # 排序预设枚举
  lib/themes/nipaplay/pages/settings/webdav_quick_settings_page.dart  # 排序设置UI
  lib/themes/cupertino/pages/settings/pages/webdav_quick_settings_page.dart  # 排序设置UI
  lib/utils/video_player_state/video_player_state_streaming.dart  # handleBackButton 改为 fire-and-forget
  lib/utils/video_player_state/video_player_state_playback_controls.dart  # 移除硬等待
  lib/pages/play_video_page.dart  # 添加 _isExiting 状态，修复 WillPopScope 路径
  lib/themes/cupertino/pages/cupertino_play_video_page.dart  # 同步修改 Cupertino 版本

注：构建生成的临时文件未列出
```

---

## 已完成功能

### 一、WebDAV 快捷 Tab 功能

#### 功能概述
在底部导航栏添加可配置的 WebDAV 快捷 Tab，方便快速访问 WebDAV 服务器中的视频文件。

#### 功能列表
1. **WebDAV Tab 开关** - 控制底部导航栏是否显示 WebDAV Tab
2. **默认服务器选择** - 从已配置的 WebDAV 连接中选择默认服务器
3. **默认目录设置** - 设置打开 WebDAV Tab 时直接进入的目录
4. **默认主页 Tab** - 设置应用启动时默认显示的 Tab
5. **WebDAV 文件浏览器** - 浏览 WebDAV 目录、播放视频
6. **文件排序预设** - 7种排序方式可选
7. **观看历史进度显示** - 在文件列表显示播放进度条和百分比

#### Tab 名称常量（用于默认主页设置）
```dart
// 在 WebDAVQuickAccessProvider 中定义
static const String tabHome = 'home';           // 首页
static const String tabVideo = 'video';         // 视频播放
static const String tabMediaLibrary = 'media_library';  // 媒体库
static const String tabAccount = 'account';     // 我的
static const String tabSettings = 'settings';   // 设置
static const String tabWebDAV = 'webdav';       // WebDAV
```

#### 回落逻辑
- 如果用户选择「WebDAV」作为默认主页，但关闭了 WebDAV Tab，应用启动时会自动回落到「首页」
- 设置页面会显示橙色警告提示用户

#### 双主题支持
- **nipaplay 主题**：用于桌面端（Windows/macOS/Linux）
- **Cupertino 主题**：用于移动端（Android/iOS），iOS 风格

---

### 二、WebDAV 文件排序预设

#### 排序选项
| 预设值 | 显示名称 | 描述 |
|--------|---------|------|
| `default` | 默认 | 文件夹在前，名称 A-Z |
| `name_asc` | 名称 A-Z | 所有项目按名称升序（例：A文件夹 → B文件 → C文件夹） |
| `name_desc` | 名称 Z-A | 所有项目按名称降序（例：Z文件夹 → Y文件 → X文件夹） |
| `modified_desc` | 最新修改 | 最近修改的项目在前 |
| `modified_asc` | 最旧修改 | 最早修改的项目在前 |
| `size_desc` | 最大文件 | 文件大小从大到小 |
| `size_asc` | 最小文件 | 文件大小从小到大 |

#### 实现位置
- `lib/providers/webdav_quick_access_provider.dart` - `WebDAVSortPreset` 枚举定义
- `lib/pages/webdav_browser_page.dart` - `_sortFiles()` 方法实现排序逻辑
- 设置页面 UI - RadioListTile 选择排序方式

---

### 三、WebDAV 观看历史进度显示

#### 功能说明
在 WebDAV 文件列表中显示视频的观看历史进度：
- 播放进度条（叠加在文件图标上）
- 进度百分比标签（显示在文件名下方）

#### 实现方式
通过 `WatchHistoryProvider` 查询历史记录：
```dart
WatchHistoryItem? historyItem;
if (videoUrl != null) {
  try {
    historyItem = historyProvider.history.firstWhere(
      (item) => item.filePath == videoUrl,
    );
  } catch (_) {
    historyItem = null;
  }
}
```

---

### 四、WebDAV UI 优化

#### 多行文件名显示
- 文件名最多显示 2 行，超出部分显示省略号
- 使用 `maxLines: 2` 和 `TextOverflow.ellipsis`

#### 返回导航逻辑修复
- 按返回键时返回到上级目录而非历史记录
- 使用路径分割获取父目录：`currentPath.substring(0, lastSlash)`
- 使用 `PopScope` 处理 Android 返回键

---

### 五、播放器防误触优化

#### 问题
- 下拉通知栏时会误触亮度调节
- 滑动操作容易误触进度调整

#### 解决方案

| 功能 | 防误触措施 |
|------|-----------|
| **亮度调节** | 顶部 48px 安全区域 + 最小滑动 10px |
| **音量调节** | 顶部 48px 安全区域 + 最小滑动 10px |
| **进度调整** | 最小水平滑动 20px |

#### 实现细节
- 手势区域顶部和底部添加安全区域，避开状态栏和导航栏
- 添加最小滑动距离阈值，只有超过阈值才开始调节
- 使用累计滑动距离判断，而非立即响应

---

### 六、播放器退出动画优化

#### 问题
- 退出播放时画面卡住一小会儿然后闪一下才退出
- 退出延迟 500-2500ms，体验不优雅

#### 根因分析
1. `handleBackButton()` 中的截图和云同步是阻塞操作（200-500ms+）
2. `resetPlayer()` 中有两个硬等待（100ms + 200ms = 300ms）
3. `AnimatedContainer` 的颜色过渡动画（300ms）在退出时导致闪烁
4. `WillPopScope` 路径没有调用 `resetPlayer()`，依赖 `dispose()` 做清理，导致状态不一致

#### 优化方案

| 优化点 | 修改内容 | 效果 |
|--------|---------|------|
| **截图和云同步** | 改为 fire-and-forget，使用 `unawaited()` 包装 | 不再阻塞退出 |
| **硬等待** | 移除 `Future.delayed(100ms)` 和 `Future.delayed(200ms)` | 减少 300ms 延迟 |
| **动画闪烁** | 退出时设置 `_isExiting = true`，禁用 `AnimatedContainer` 动画 | 消除颜色过渡闪烁 |
| **路径统一** | `WillPopScope` 路径添加 `resetPlayer()` 调用 | 状态清理一致 |

#### 优化效果
- **优化前：** 退出延迟 500-2500ms，有闪烁
- **优化后：** 退出延迟 <100ms，无闪烁，动画平滑

#### 实现细节
- `handleBackButton()` 使用 `unawaited()` 包装截图和云同步，立即返回 `true`
- `resetPlayer()` 移除两个 `Future.delayed` 硬等待
- 添加 `_isExiting` 状态变量，退出时禁用 `AnimatedContainer` 动画
- 统一两条退出路径（系统返回键和 UI 按钮），都调用 `resetPlayer()`

#### 修改文件
- `lib/utils/video_player_state/video_player_state_streaming.dart` - handleBackButton 改为 fire-and-forget
- `lib/utils/video_player_state/video_player_state_playback_controls.dart` - 移除两个硬等待
- `lib/pages/play_video_page.dart` - 添加 _isExiting 状态，修复 WillPopScope 路径
- `lib/themes/cupertino/pages/cupertino_play_video_page.dart` - 同步修改 Cupertino 版本

---

## 文件清单

### 新建文件（共 7 个）

| 文件路径 | 用途 |
|---------|------|
| `1.md` | 项目进度记录文件 |
| `CLAUDE.md` | Claude Code 项目指南 |
| `lib/providers/webdav_quick_access_provider.dart` | WebDAV 快捷设置 Provider、排序预设枚举 |
| `lib/pages/webdav_browser_page.dart` | WebDAV 文件浏览器页面、排序、历史进度 |
| `lib/themes/nipaplay/pages/settings/webdav_quick_settings_page.dart` | nipaplay 主题设置页面 |
| `lib/themes/cupertino/pages/settings/widgets/webdav_quick_setting_tile.dart` | Cupertino 设置入口 Tile |
| `lib/themes/cupertino/pages/settings/pages/webdav_quick_settings_page.dart` | Cupertino 主题设置页面 |

### 修改文件（共 13 个）

| 文件路径 | 修改内容 |
|---------|---------|
| `lib/main.dart` | 注册 WebDAVQuickAccessProvider，动态 Tab 支持，默认 Tab 选择 |
| `lib/pages/tab_labels.dart` | 添加 showWebDAVTab 参数，动态 Tab 标签 |
| `lib/themes/nipaplay/pages/settings_page.dart` | 添加 WebDAV 快捷设置入口 |
| `lib/themes/cupertino/pages/cupertino_main_page.dart` | 动态 WebDAV Tab 支持，默认 Tab 选择 |
| `lib/themes/cupertino/pages/settings/sections/cupertino_settings_general_section.dart` | 添加 WebDAV Tile |
| `lib/themes/nipaplay/widgets/volume_gesture_area.dart` | 防误触区域和最小滑动阈值 |
| `lib/themes/nipaplay/widgets/brightness_gesture_area.dart` | 防误触区域和最小滑动阈值 |
| `lib/themes/nipaplay/widgets/video_player_ui.dart` | 水平滑动最小距离阈值 |
| `lib/utils/video_player_state/video_player_state_streaming.dart` | handleBackButton 改为 fire-and-forget |
| `lib/utils/video_player_state/video_player_state_playback_controls.dart` | 移除两个硬等待（100ms + 200ms） |
| `lib/pages/play_video_page.dart` | 添加 _isExiting 状态，修复 WillPopScope 路径 |
| `lib/themes/cupertino/pages/cupertino_play_video_page.dart` | 同步修改 Cupertino 版本 |
| `pubspec.lock` | 依赖版本锁定 |

---

## GitHub 发布记录

### v1.0.2 (2026-04-27)
- 优化：播放器退出动画，消除卡顿和闪烁
- 优化：截图和云同步改为 fire-and-forget，不阻塞退出
- 优化：移除 resetPlayer() 中的硬等待（100ms + 200ms）
- 优化：退出时禁用 AnimatedContainer 动画，避免颜色过渡闪烁
- 修复：统一两条退出路径，都调用 resetPlayer() 进行状态清理

### v1.0.1 (2026-04-27)
- 新增：7种文件排序预设（默认、名称A-Z、名称Z-A、最新修改、最旧修改、最大文件、最小文件）
- 新增：WebDAV 文件列表显示观看历史进度条和百分比
- 新增：多行文件名显示（最多2行）
- 修复：返回键导航逻辑（返回上级目录而非根目录）

### v1.0.0 (2026-04-27)
- 新增：WebDAV 快捷 Tab 功能
- 新增：默认主页 Tab 设置
- 新增：播放器防误触优化
- Android APK 签名支持

---

## 构建输出

| 平台 | 输出路径 |
|------|---------|
| **Linux** | `build/linux/x64/release/bundle/NipaPlay` |
| **Android** | `build/app/outputs/flutter-apk/app-release.apk` (约 110MB) |

### 构建命令

```bash
# Linux 构建
flutter build linux

# Android 构建（使用本地 SDK）
export ANDROID_HOME=~/android-sdk
flutter build apk
```

### Android 签名配置
签名配置文件：`android/key.properties`（不纳入版本控制）
```properties
storePassword=密钥库密码
keyPassword=密钥密码
keyAlias=nipaplay-release
storeFile=/path/to/nipaplay-release.keystore
```

---

## 重要技术细节

### 1. 主题系统
项目使用双主题系统：
- **nipaplay**：Material 风格，用于桌面端
- **Cupertino**：iOS 风格，用于移动端（Android/iOS 默认使用此主题）

**注意**：Android 默认使用 Cupertino 主题，修改设置页面时需要同时修改两个主题的代码。

### 2. 状态管理
使用 Provider 模式进行状态管理：
- `WebDAVQuickAccessProvider` - 管理 WebDAV 快捷设置
- `VideoPlayerState` - 播放器状态核心
- 设置存储使用 `SharedPreferences`

### 3. Tab 结构

**nipaplay 主题 Tab 顺序**：
1. 首页 (index 0)
2. 视频播放 (index 1)
3. WebDAV (index 2，动态)
4. 媒体库 (index 3/2)
5. 我的 (index 4/3)

**Cupertino 主题 Tab 顺序**：
1. 首页 (index 0)
2. WebDAV (index 1，动态)
3. 媒体库 (index 2/1)
4. 我的 (index 3/2)
5. 设置 (index 4/3)

### 4. 手势控制层级

```
GestureDetector (整个播放区域)
├── onHorizontalDragStart/Update/End → 进度调整（需滑动 >20px）
├── onVerticalDragStart/Update/End → 被 VolumeGestureArea 和 BrightnessGestureArea 拦截
├── onTap → 显示/隐藏控制栏
├── onDoubleTap → 播放/暂停
└── onLongPress → 倍速播放
```

---

## 待办事项

无

---

*最后更新：2026-04-27*
